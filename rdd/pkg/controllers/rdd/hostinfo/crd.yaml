# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: SUSE LLC
# SPDX-FileCopyrightText: The Rancher Desktop Authors
# Generated by generate-crds.sh; DO NOT EDIT.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: hostinfos.rdd.rancherdesktop.io
spec:
  group: rdd.rancherdesktop.io
  names:
    categories:
    - all
    kind: HostInfo
    listKind: HostInfoList
    plural: hostinfos
    singular: hostinfo
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.cpus
      name: CPUs
      type: integer
    - jsonPath: .status.memory
      name: Memory
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          HostInfo is a cluster-scoped singleton that exposes host hardware limits
          (CPU count and total memory) so that clients such as the GUI can determine
          valid ranges for VM resource settings without inspecting the host directly.
          The controller creates and maintains exactly one instance named "system".
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              HostInfoSpec is empty; HostInfo is a read-only singleton managed entirely
              by the controller.
            type: object
          status:
            description: HostInfoStatus reports the detected host hardware limits.
            properties:
              cpus:
                description: cpus is the number of logical CPUs on the host.
                type: integer
              memory:
                description: memory is the total host RAM in bytes.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
